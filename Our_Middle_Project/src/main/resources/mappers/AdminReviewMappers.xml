<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AdminReviewMappers">

	<select id="selectAllReviews"
		resultType="com.our_middle_project.dto.AdminReviewDTO">
		SELECT
		B.BOARD_NO AS boardNo,
		B.BOARD_TITLE AS boardTitle,
		B.BOARD_CONTENT AS boardContent,
		M.NICKNAME AS nickname,
		TO_CHAR(B.CREATED_DATE, 'YYYY-MM-DD') AS createdDate,
		S.STAR AS stars,
		CASE WHEN I.FILE_NO IS NOT NULL THEN 'Y' ELSE 'N' END AS hasImage,
		R.REPLY_CONTENT AS adminReply,
		TO_CHAR(R.CREATE_DATE, 'YYYY-MM-DD') AS adminReplyDate

		FROM
		BOARD B
		JOIN MEMBER M ON B.MEM_NO = M.MEM_NO
		LEFT JOIN BOARD_STAR S ON B.BOARD_NO = S.BOARD_NO
		LEFT JOIN BOARD_IMAGE I ON B.BOARD_NO = I.BOARD_NO
		LEFT JOIN BOARD_REPLY R ON B.BOARD_NO = R.BOARD_NO AND R.UP_REPLY_NO IS
		NULL

		WHERE
		B.TYPE_NO = 2 ORDER BY
		B.CREATED_DATE DESC
	</select>

</mapper>