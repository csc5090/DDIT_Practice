<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.our_middle_project.dao.MemberDAO">

	<select id="getMemberList" parameterType="java.lang.String"
		resultType="com.our_middle_project.dto.MemberDTO">
		SELECT
		USER_NO AS userNo,
		USER_ID AS userId,
		NICKNAME,
		REG_DATE AS regDate,
		STATUS FROM MEMBER
		<if test="query != null and query != ''">
			WHERE USER_ID LIKE CONCAT('%', #{query}, '%') OR NICKNAME LIKE CONCAT('%', #{query}, '%')
		</if>
		ORDER BY USER_NO DESC
	</select>
</mapper>