const StatsPage = {
    init: function() {
        if (!document.getElementById('stats-main')) return;

        console.log("데이터/통계 페이지 초기화 완료.");
        // 나중에 여기에 데이터/통계 페이지 전용 이벤트 리스너 등을 추가.
    }
};